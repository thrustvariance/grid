# Artifacts

This repository intentionally stores **text-based artifacts only** (no committed binary files).

During execution, visual screenshots may be generated locally to:
- `/tmp/webapp-homepage.png` (ephemeral)

For repository-safe evidence, the automation also writes:
- `artifacts/webapp-homepage.html` (captured homepage HTML)
- `artifacts/execution-report.txt` (runtime summary)
